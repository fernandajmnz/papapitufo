import {test, expect} from '@playwright/test';

test.describe('mercado libre', () => {
  test('encontrar productos usando locators', async ({  }) => {
  
    await page.goto('https://www.mercadolibre.com.mx/');
    

  });
});